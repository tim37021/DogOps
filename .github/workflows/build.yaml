name: Build
run-name: Build DogOps 🚀
on: [push]
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsSL https://bun.sh/install | bash
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: $HOME/.bun/bin/bun install
      - run: $HOME/.bin/bin/bun run build